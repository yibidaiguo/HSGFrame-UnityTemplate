# 创作管线 · P7 批次 3「面板收尾」日志

> 总账在 [创作管线进度](../创作管线进度.md)。本文只装这一批的落地记录与验收证据。
> **这是方案定义范围内的最后一批。**

## 这一批真正做的是什么

子文档 04 §二那张页与数据源表里最后没做的三项：
**下游页**（新增一页，面板到十六页）、**设计池的时间线与定稿预览**（扩页）、
**资产页的离风格列**（扩页，接 P7 批次一的引擎）。

## 密钥只报「配没配」，值一次都不读

这一批最硬的一条红线是决策 5。`ReadBridges` 判密钥配没配时**只调
`TryGetProperty(..., out _)` 看键在不在，一次都不取 value**——
不放进任何返回字段、不写日志、不回显、不输出长度。
面板给密钥字段**不给输入框、不给保存按钮**：保存等于把密钥落盘，那不是面板该做的事。

## 离风格按行按需算，不在页面加载时全算

解一张 PNG 要真跑 inflate + 反滤波 + k-means。一页几十上百个资产，
加载时全算会把面板卡死。所以资产页的离风格列默认是「未算」+ 一个按钮，
点了才打 `/api/panel/deviation?requirement=…&asset=…`。
**没有预览图 / 没有风格锚点的行按钮直接禁用**，省一次注定失败的往返。

## 「没对账」不是「零依赖全满足」

线上 driver 不做本地能力对账，`CapabilityMeasured` 为 false；
探测输出文件不存在也是 false，两个计数写 **-1 而不是 0**——
0 会被读成「零个依赖，全满足」（决策 42）。
页面在 `CapabilityMeasured` 为 false 时**一个数字都不显示**，只给原因。

## 验收时抓到的两个真问题

### 一、定稿预览永远显示「还没有定稿」

`ReadDesigns` 只扫 `Designs/<分类>/*.json` 这一层平铺文件。
但**定稿是一稿一目录**：`Designs/定稿/<名>/定稿.json`——
子文档 06 §五这么写，P7 批次一的 `FinalPalette.Load` 也是照这个找的。

于是本批新加的色板、定稿版本、参考图三个字段，**挂在一种永远不会出现的行上**。
我铺了一份真定稿进去开面板看，那一块果然是「还没有定稿」。

修：定稿这一类额外扫一层子目录取 `定稿.json`；名称取**目录名**，
不取恒为「定稿」的文件名。补了
`FinalInItsOwnDirectoryIsFoundAndNamedAfterTheDirectory` 盯着。

**这个 bug 的形状和 P7 批次二那个面板 JS 一样**：功能写好了、测试全绿、
门禁全绿，但那条路上根本不会有数据经过——**只有真开一次面板才看得见**。

### 二、决策 5 的落盘保护从 P1 起就是一句空话

执行后端报上来的：`git check-ignore Config/创作管线/本机.json` 返回 exit=1，
**`.gitignore` 里根本没有这一条**。

决策 5 白纸黑字写着「`Config/创作管线/本机.json` 进 `.gitignore`」，
从 P1 写到现在**没有人真的去补那一行**。只要哪天有人生成了本机配置，
密钥就会进 git——正是这条决策要防的事。已补上。

## 落地

**读取器**：`ReadBridges`（下游）、`ReadDeviation`（按需离风格）、
扩 `ReadDesigns`（时间/退化标志/色板/定稿版本/参考图 + 定稿子目录扫描）、
扩 `ReadAssets`（预览路径/风格锚点）。

**页面**：新增 `渲染下游`；`渲染设计池` 改成定稿区 + 时间线；
`渲染资产` 加离风格列与按需按钮。**面板到十六页。**

**路由**：`/api/panel/bridges`、`/api/panel/deviation`（中文与 ASCII 参数别名都认，
缺参返回带原因的「没算成」而不是空对象——决策 42/57）。

**`.gitignore`**：补 `Config/创作管线/本机.json`（决策 5 的落地）。

## 本批新增的锁定决策

78 – 79 见 [创作管线锁定决策](../创作管线锁定决策.md)：
密钥只判键在不在、定稿是一稿一目录。

## 验收记录（全部由 Claude 亲自复跑，不采信执行后端的自述）

| 项 | 结果 |
|---|---|
| `dotnet build Solutions/Template.sln` | 0 警告 0 错误 |
| `dotnet test Solutions/Template.sln` | 全绿；`Dashboard.Tests` **108 条**（88 → 108） |
| `pwsh Tools/Gates/gate.ps1` 全量 | **PASS**（修完命名门禁之后） |
| `CreationPanelScriptTests` | 两条绿——上一批加的那道拦网这一批就派上用场了 |
| 工作区残留 | `Pools/Designs/` 下**只剩三个 `.gitkeep`**，临时验收数据已清 |

**密钥诱饵真跑**（这一批最重要的一条）：往 `Config/创作管线/本机.json`
放一个值是 `CLAUDE_ACCEPTANCE_CANARY_9f3a` 的密钥，然后——

| 查哪里 | 结果 |
|---|---|
| `/api/panel/bridges` 的完整响应 | **诱饵一次都没出现** |
| 整份面板 HTML | **0 次** |
| `git status` | **完全看不见这个文件**（`.gitignore` 补上了） |

**真把面板开起来看过**（`--port 8767 / 8768`，浏览器实开，控制台**零错误**）：

| 验的什么 | 结果 |
|---|---|
| 页签数 | **十六个**，原十五个的键名与顺序逐字未动 |
| 下游页 | 四个 driver 卡片真渲染；blender 的能力对账写「找不到探测输出文件」，**一个数字都没显示** |
| 密钥提示语 | 「密钥只显示配没配，值不经过面板」印在页面上 |
| 设计池·定稿区 | 修完子目录扫描后：`临时验收风格@v3` + **三个真色块**（`background-color` 的 span）+ 参考图路径 |
| 设计池·时间线 | 有「时间」字段的按原值，没有的显示文件时间并标 **`(文件时间)`** 灰字 |
| 抠出 `<script>` 跑 `node --check` | **语法通过** |

## 验收时我自己修的四处

1. **定稿子目录扫描**（上面那节）——本批功能的死路，修+补测试。
2. **`.gitignore` 缺 `本机.json`**（上面那节）——P1 就欠着的账。
3. **命名门禁红 17 条**：新测试里写了一堆**中文局部变量名**（`var 地址 = …`）。
   这是真违规不是误报（不同于上一批那个单行 raw string 的假红），改成英文即绿。
   执行后端仍然只跑了 build/test 没跑全量门禁——**这条我下次要写进任务书的验收命令**。
4. **`创建时间` 这个来源被我自己的任务书写没了**：原本 `ReadDesigns` 是
   `时间` → `创建时间` 两级，我的任务书只写了「`时间` 取不到就用文件时间」，
   执行后端照做，`创建时间` 就被挤掉了。作者写下的创建时间比「文件上次被谁碰过」准，
   已恢复成 `时间` → `创建时间` → 文件时间三级，并把那条被改的断言改了回去。

## 执行后端这一轮报上来的

1. **它指出我的任务书自相矛盾**：追加节要求「自己开一次面板看」，
   而红线与环境节写着「不许起面板服务」。**它按红线办并把这件事交回给我**——
   判断正确，那一页确实由我来开。这是我任务书的 bug。
2. **上一批加的 `CreationPanelScriptTests` 当场帮它抓到了自己的错**：
   它新写的 JS 有跨行拼接导致引号落单，测试报了行号，它合并成单行后转绿。
   **拦网上一批刚装，这一批就拦住了一次。**
3. **它发现了 `.gitignore` 的缺口**并如实报上来，没有自作主张去改
   （`.gitignore` 不在它的改动表里）。这个处理是对的。
4. 密钥不外泄那条测试它做了破坏性验证：改成泄漏版当场红，改回即绿。

## 已知缺口（不阻塞，记着）

- **能力探测输出的路径是执行后端定的**（`_Generated/Bridges/<名>/探测结果.json`），
  CLI 的 `art.caps` 走的是显式 `ProbeResultPath` 参数。两边**没有约定同一个默认值**，
  真要用得先统一。
- **下游页的配置表单只读**，改配置仍要手工编辑文件（决策 5 的必然结果）。
- **参考图只列路径不加载图**，面板不做图片代理。
- **资产页的离风格要一行一行点**，没有「全算」按钮——那会把面板卡住。
- **`ReadDeviation` 每次都重算**，没有缓存（面板零私有状态，子文档 04 §一）。
  同一行点两次会解两次 PNG。
- **设计池的「记录时间线」没有分页**，记录多了会很长。
