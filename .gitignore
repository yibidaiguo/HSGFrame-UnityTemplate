# 仓库根 .gitignore 全局忽略了 *.csproj / *.sln，这里把 Template 自建的工程文件重新纳入
!*.csproj
!*.sln

# 编译产物
bin/
obj/

# 命令层运行日志（unity-cmd.ps1 落盘用）
Logs/

# 打包中间产物（pack-hotfix.ps1 的清单与包目录）
Build/

# Unity 生成物（人第一次打开工程时才长出来）
UnityProject/Library/
UnityProject/Temp/
UnityProject/Logs/
UnityProject/obj/
UnityProject/Build/
UnityProject/Builds/

# YooAsset 模拟构建与真构建的资源包产出：由「工具链/资源/模拟构建资源包」现产，不进仓库。
UnityProject/Bundles/
UnityProject/MemoryCaptures/

# HybridCLR 的本地 il2cpp 数据（约 800 MB）：由「初始化开发环境」现装，不进仓库。
UnityProject/HybridCLRData/

# 命令层断点文件与一次性参数文件的落脚处，不进仓库。
Temp/

# Luban 命令行工具（约 6 MB，197 个文件）：由 Tools/Luban/fetch-tool.ps1 现取，不进仓库。
# 与 HybridCLR 同一手法——第三方工具链按需装，仓库里只留取它的办法与版本号。
Tools/Luban/*
!Tools/Luban/fetch-tool.ps1
!Tools/Luban/SOURCE.md
# Luban 的表格工程（原 Tools/Luban/Config/）：这是仓库自己的数据，不是下下来的工具，
# 所以要从上面那条 Tools/Luban/* 里放行出来（待办 2：工具链配置归工具目录）。
!Tools/Luban/Config/
!Tools/Luban/Config/**

# 出包前现拷的热更程序集与 AOT 补充元数据（HotfixShipStaging 产出），是构建产物不是源码。
UnityProject/Assets/StreamingAssets/HotfixShip/
UnityProject/Assets/StreamingAssets/HotfixShip.meta

# Unity 侧的工程文件由 Rider 包（com.unity.ide.rider）现生成，是产物不是源码。
# 顶上的 !*.csproj / !*.sln 是为 Solutions/ 里手写的工程放行的，这里把 Unity 生成的那批重新排除。
UnityProject/*.csproj
UnityProject/*.sln
UnityProject/.idea/

# 模型试验区：AI 本地测试的临时文件全放这，永不上传；只放行说明文件让规则随仓库走。
# 模板自己就是仓库根时这条直接生效；模板是别人的子目录时，试验区在那个仓库的根上，各自忽略各自的。
_Scratch/*
!_Scratch/README.md

# 生图/建模的变体本体不进仓库，边车进——变体可由边车重生成，算力换体积
# （创作管线子文档 06 §二 第 8 步）。目录要放行，否则 git 不会往里走。
_Tasks/*/30-outputs/*/变体/**
!_Tasks/*/30-outputs/*/变体/**/
!_Tasks/*/30-outputs/*/变体/**/*.provenance.json

# 引擎守护的运行时状态跟机器走，不跟仓库走（创作管线锁定决策 81、82）：
# 轮次账本是这台机器跑了什么的流水，锁文件带的是本机进程号，
# 唤醒信号与它的归档是这台机器收到过什么。进 git 只会让两台机器互相打架。
_Tasks/engine-ticks.jsonl
_Tasks/.prereview-cache/

# 能力探测结果是「这台机器上装了什么」，跟机器走不跟仓库走，所以不进 git。
# 与 _Generated/Bridges/（供给产物，进 git 供对账——决策 12）刻意分开两个目录：
# gate.provision 靠 _Generated/Bridges/<driver>/ 在不在判断该 driver 声称已供给，
# 探测结果混进去会让它判红（P8 批次 8 真踩过）。
_Generated/Probes/
_Tasks/.engine.lock
_Tasks/wake/
# 助手常驻会话的运行时状态同理：收到的消息、发出去的草稿留底与会话流水都是
# 「这台机器上发生过什么」，跟机器走不跟仓库走（决策 95）。
_Tasks/conversations/

# 下游的密钥配置永不入库（创作管线锁定决策 5）。共享的 downstream.json 只存密钥「名」，
# 可以进 git；local.json 存的是值，一进 git 就是决策 5 要防的那件事。
# 这条从 P1 就写在决策里，但直到 P7 批次三才真的补上——在此之前它只是一句话。
Tools/CreationPipeline/Config/local.json
