<?xml version="1.0" encoding="utf-8"?>
<!--
  还原只走仓库自带的本地源。**执行机不联网也要能编**，这份配置是那件事的开关。

  <clear /> 是有意的：不清掉 nuget.org，「离线能编」就只是「本机缓存里恰好有」，
  换台机器才炸——而那正是内置化要根治的症状。清掉之后，
  少了什么会在还原阶段当场说清楚（NU1101：某某包在本地源里没有），
  而不是等到运行时才发作。

  要加一个新包，走这条路，别改这份文件：
    1. 能铺平成 dll 的（只有程序集）→ 写进 Tools/Deps/packages.txt
    2. 带 targets / 分析器 / 测试宿主的 → 写进 Tools/Deps/packages-feed.txt
    3. pwsh -NoProfile -File Tools/Deps/fetch.ps1   ← 这一步要联网，只此一次
    4. 提交 Tools/Deps/ 下多出来的东西（dll 与 nupkg 都进 git，这是「内置」的含义）

  取依赖的那一次联网由 fetch.ps1 完成，它在系统临时目录里建工程，
  看不到这份配置，所以清掉 nuget.org 不影响它重取。

  许可账在 Tools/Deps/LICENSES.md，出处与平台取舍在 Tools/Deps/SOURCE.md。
-->
<configuration>
  <packageSources>
    <clear />
    <add key="内置依赖" value="Tools/Deps/nupkg" />
  </packageSources>
</configuration>
