## 本项目由通用 Unity 模板生成

- 项目名：{{项目名}}
- 框架包沿用 `com.hsgframe.*` / `HSGFrame.*`：HSGFrame 是框架自己的名字，不跟项目走
- 命名空间：`{{项目名}}.*`（生成时已由 project.create 从模板的 `Template.*` 整体替换）

跑门禁：`./{{项目名}}/Tools/Gates/gate.ps1`

第一次出包前先重编源生成器：`Tools/SourceGenerators/HotfixProbe` 的产物
`{{项目名}}.Hotfix.Analyzer.dll` 是随模板拷过来的那一份，里面写的还是模板的命名空间。
按 `Tools/SourceGenerators/HotfixProbe/来源说明.md` 重编一次再拷进
`UnityProject/Assets/_Project/Scripts/Hotfix/`，热更验收才对得上。
