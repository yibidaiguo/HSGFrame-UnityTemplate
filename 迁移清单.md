# 从旧工程搬了什么

旧工程 `RPG_Unity/`（137 个 `.cs`，其中 91 个属于 JKFrame 第三方框架）转为只读归档。
按方案第五节 B 第 3 项，只搬三样，其余重写或放弃。

| 搬了 | 来源 | 怎么处理 |
|---|---|---|
| 计时器工具 | `Assets/Scripts/FrameWork/Utility/GameTime/`（6 个文件 425 行） | **重写**成 `Packages/com.gametemplateforagent.timer/`：时间由 `Tick(deltaSeconds)` 从外部推进，不再直接读 `UnityEngine.Time.time`，因此能在 `dotnet test` 下跑；公开字段改成属性，`id` / `delay` 一类改成 `TaskId` / `IntervalSeconds` |
| Luban 配置 | `Config/Luban/MiniTemplate/` | 原样复制到 `Config/Luban/MiniTemplate/`。它是数据与工具配置，不含命名问题 |
| Jenkins 出包脚本 | `Assets/Scripts/Rpg/Editor/Jenkins*BuildCli.cs` | **重写**成 `UnityProject/Assets/_Project/Scripts/Editor/PlayerBuildCommandLine.cs`：两个类合并成一个，加命名空间，`BuildWindows` / `BuildAndroid` 两个入口 |

## 没搬的

| 没搬 | 为什么 |
|---|---|
| JKFrame 整套（91 个文件：池 / 事件 / 资源 / 音频 / 存档 / UI / 本地化 / 日志） | 方案的既定决策就是剥离它；模板已有对应的可选包（存档 / 本地化 / 音频 / 对象池），按需长 |
| `Assets/Gen/Code/`（Luban 生成的 4 个 `.cs`） | 生成物，重跑 Luban 即可，不搬 |
| 旧 UI / 业务脚本 | 旧工程几乎是空的（1 个空场景、0 个业务 Prefab），没有值得搬的业务内容 |
| Addressables 配置 | 模板走 YooAsset（方案模块 13），两套资源方案不混用 |
