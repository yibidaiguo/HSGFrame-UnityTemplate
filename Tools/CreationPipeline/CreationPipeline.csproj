<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>disable</Nullable>
    <LangVersion>latest</LangVersion>
    <AssemblyName>Toolkit.CreationPipeline</AssemblyName>
    <RootNamespace>Template.Toolkit.CreationPipeline</RootNamespace>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <NoWarn>CS1591</NoWarn>
  </PropertyGroup>

  <!--
    Svg.Skia：把布局图的 SVG 转成 PNG。
    飞书文档的图片块只收位图，SVG 塞不进去；而 SVG 那份要留着进 git 看 diff。
    **选 Svg.Skia 而不是裸 SkiaSharp**：我们已经在渲 SVG，转一道能保证两张图
    天然一致；自己再画一遍 PNG 就是两套渲染器，迟早漂成两个样子。
    它带进 SkiaSharp 的原生库（libSkiaSharp.dll + libHarfBuzzSharp.dll，win-x64 一份约 14 MB）——
    这是这个仓库第一个原生依赖，值得记一笔：换来的是不必自带点阵字，且将来标中文也能画。

    SixLabors.ImageSharp：位图的编解码（PNG 读写、动图 GIF 写出）。
    **不自己手写编解码器**——GIF 的 LZW 与 PNG 的 zlib/滤波都是「写得出来但养不起」的东西：
    出一个位错，症状是某台机器上某张图花掉，而查起来要从字节流读起。
    钉 2.1.x 是有意的：**2.1 是 Apache-2.0 的最后一支**，3.x 起换成 Six Labors 分割许可
    （开源与小企业免费，商用要买）。升它等于换许可，不是版本号问题。

    两个都是**内置依赖**，不走 PackageReference：dll 在 Tools/Deps/lib/，
    原生库在 Tools/Deps/native/win-x64/，版本在 Tools/Deps/packages.txt，
    许可在 Tools/Deps/LICENSES.md。这样执行机不联网也能编——
    理由与重取办法见 Tools/Deps/SOURCE.md。
  -->
  <Import Project="../Deps/props/ImageSharp.props" />
  <Import Project="../Deps/props/SvgSkia.props" />

</Project>
